<template name="menuItem" >
  <navigator class="menu-item" url="../productList/productList?productName={{item.name}}">
    <image class="menu-icon" src="{{item.icon}}" />
    <text class="menu-name">{{item.name}}</text>
  </navigator>
</template>